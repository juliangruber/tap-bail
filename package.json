{
  "name": "tap-bail",
  "version": "0.0.0",
  "description": "Bail out when the first TAP test fails",
  "repository": "juliangruber/tap-bail",
  "license": "MIT",
  "scripts": {
    "test": "make test"
  },
  "bin": {
    "tap-bail": "bin/tap-bail.js"
  },
  "devDependencies": {
    "tap": "^8.0.1"
  },
  "dependencies": {
    "tap-parser": "^3.0.4"
  }
}